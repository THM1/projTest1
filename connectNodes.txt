try create an array of all the centre points
load them all into a buffer

create indices type

loop through each node and select two indices

use DrawElements(enum mode, sizei count, enum type, void* indices);
